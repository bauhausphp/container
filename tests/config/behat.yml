default:
   autoload:
      '': %paths.base%/../acceptance/

   suites:
      defaults:
         contexts: []

      readable_container:
         paths:
            - %paths.base%/../acceptance/features/
         contexts:
            - Bauhaus\Container\ReadableContainerUserContext
         filters:
            role: readable container user
